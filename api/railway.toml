[build]
builder = "NIXPACKS"

[deploy]
numReplicas = 1
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[source]
branch = "claude/decision-os-revenue-wedge-RxOLJ"
