[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build --workspace=@bebrahma/web']

[start]
cmd = 'npm run start --workspace=@bebrahma/web'
