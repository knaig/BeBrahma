[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build --workspace=@bebrahma/api']

[start]
cmd = 'npm run dev --workspace=@bebrahma/api'
